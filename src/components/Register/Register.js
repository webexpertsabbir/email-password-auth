import React from 'react';

const Register = () => {
    return (
        <div>
            {/* <form onSubmit={handelRegister}>
            <input onBlur={handelEmailBlur} type="email" name="email" id="1" placeholder='Enter your Email' />
            <br />
            <input onChange={handelPasswordChange} type="password" name="password" id="2" placeholder='Enter Your Password' />
            <br />
            <button type="submit">Register</button>
          </form> */}
        </div>
    );
};

export default Register;